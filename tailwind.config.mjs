/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue,json}'],
  darkMode: 'media',
  // Tailwind CSS v4 uses CSS-based configuration instead of JS config
  // Most customizations are now handled in CSS with @theme directive
};
